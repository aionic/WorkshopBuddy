Revise the existing artifact using the user's instructions.

Existing artifact:
{{artifactMarkdown}}

User revision instructions:
{{revisionInstructions}}

Keep the artifact aligned to the original project context and workshop inputs. Improve clarity, structure, executive tone, and usefulness. Return the full revised artifact as structured JSON matching the schema: { "title": string, "subtitle": string, "sections": [{ "heading": string, "content": string }], "assumptions": string[], "nextSteps": string[] }
